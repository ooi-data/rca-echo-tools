name: ooi-hyd-tools
prefect-version: 2.14.15

# build section allows you to manage and build docker images
build:

# push section allows you to manage if and how this project is uploaded to remote locations
push:

# pull section allows you to provide instructions for cloning this project in remote locations
pull:
- prefect.deployments.steps.git_clone:
    repository: https://github.com/ooi-data/rca-echo-tools.git
    branch: main

# the deployments section allows you to provide configuration for deploying flows
deployments:
- name: echo_tools_8vcpu_60gb
  version:
  tags: []
  description:
  entrypoint: ./rca_echo_tools/harvest.py:echo_raw_data_harvest
  parameters: {}
  work_pool:
    name: hydrophone-pool
    work_queue_name:
    job_variables:
      cpu: 8192
      memory: 61440
      image: jdduprey/rca-echo-tools:latest # overwrites the default image associated with the workpool
  schedule:
  is_schedule_active: true
- name: echo_tools_16vcpu_80gb
  version:
  tags: []
  description:
  entrypoint: ./rca_echo_tools/harvest.py:echo_raw_data_harvest
  parameters: {}
  work_pool:
    name: hydrophone-pool
    work_queue_name:
    job_variables:
      cpu: 16384
      memory: 81920
      image: jdduprey/rca-echo-tools:latest # overwrites the default image associated with the workpool
  schedule:
  is_schedule_active: true
  

